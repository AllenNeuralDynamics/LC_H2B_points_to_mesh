#!/usr/bin/env bash
set -euxo pipefail
# Master script for the capsule's "Reproducible Run".
#
# Generate the LC core mesh and the nine percentile meshes from the raw cell point
# calls. Writes new_core_mesh.obj, percentile_{10..90}.obj, and a JSON report.

python -u reproduce_meshes.py
