#!/usr/bin/env bash
set -euxo pipefail
# Master script for the capsule's "Reproducible Run".
#
# Regenerate the LC core mesh from the raw cell points and compare it to the
# published mesh. Writes new_core_mesh.obj, a side-by-side comparison figure, and a
# JSON report to ../results/reproduced/.

python -u reproduce_core_mesh.py
