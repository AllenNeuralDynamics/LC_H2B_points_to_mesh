# Top-level (direct) dependencies of the LC mesh capsule, pinned to the versions in the
# original mesh-generation environment. Compile the fully-pinned lock (requirements.txt)
# from this with:
#   uv pip compile environment/requirements.in --python-version 3.10 \
#       --python-platform linux --exclude-newer 2026-02-03 -o environment/requirements.txt
# mesh generation
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
trimesh==4.11.1
open3d==0.18.0
point-cloud-utils==0.34.0
tqdm==4.67.3
# figures + notebook
matplotlib==3.10.8
plotly==6.5.2
ipywidgets==8.1.8
# raw-image max-projection overlay figure
zarr==2.18.3
tifffile==2025.5.10
