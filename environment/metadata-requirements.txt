# Dependencies for the SEPARATE metadata environment (/opt/meta-env), used only by
# code/write_metadata.py to emit the mesh asset's data_description.json + processing.json.
#
# aind-data-schema is a current release and is intentionally NOT part of the pinned geometry
# environment (environment/requirements.txt): it lives in its own env so it cannot affect
# mesh reproducibility, and so the geometry env stays minimal. The default aind-data-schema-models
# version (models<6) is fine here (the mesh asset's institution is AIND, not Stanford).
aind-data-schema==2.8.0
